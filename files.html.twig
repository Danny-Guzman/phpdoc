{% extends "vendor/caweb/phpdoc/index.html.twig" %}

{% block head %}
    <title>CAWeb Files</title>
{% endblock head %}

{% block introduction %}

<h1>Files</h1>

{% endblock introduction %}

{% block table_of_contents %}
{% set __index = '' %}
{% for file in project.files|sort_asc %}
    {% set __new_index = file.name|first matches '/[0-9]/' ? '#' : file.name|first|upper %}
    {% if __index != __new_index %}
    {% set __index = __new_index %}
    <strong>{{ __new_index }}</strong>
    {% endif %}
<p><a href="{{ file|route(presentation = 'url') }}">{{ file.name }}</a></p>
{% endfor %}

{% endblock table_of_contents %}

{% block body %}

{% endblock body %}
